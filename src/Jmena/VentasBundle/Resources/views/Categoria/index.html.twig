{% extends '::base.html.twig' %}

{% block title%}
    Listado de Categorias
    {{ parent() }}
{% endblock%}

{% block body -%}
    <h2>Listado de Categorias</h2>
    <a class="new btn btn-primary btn-large" href="{{ path('categoria_new') }}">NUEVA CATEGORIA</a>
    </br>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Descripción</th>
                <th>Detalle</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.descripcion }}</td>
                <td>
                    <a href="{{ path('categoria_show', { 'id': entity.id }) }}">
                        <i class="icon-eye-open"></i> IR
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}